{
  "name": "@feel/intellij-plugin",
  "version": "0.0.2",
  "description": "IntelliJ IDEA plugin for FEEL language support",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm run build:binaries && pnpm run bundle:binaries && ./gradlew buildPlugin --no-configuration-cache",
    "build:binaries": "cd ../lsp-server && pnpm build:binaries",
    "bundle:binaries": "node scripts/bundle-server.js",
    "clean": "./gradlew clean",
    "dev": "./gradlew runIde --no-configuration-cache",
    "verify": "./gradlew verifyPlugin"
  },
  "keywords": [
    "feel",
    "intellij",
    "plugin",
    "lsp"
  ],
  "author": "FEEL Language Team",
  "license": "ISC"
}
